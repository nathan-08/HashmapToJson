Serialize a hashmap of \<string, int\> to json, write to file, then read from file and deserialize into hashmap.


Useful for storing persistent data, such as a symbol table as an artifact of an assembly process. 
